# Document AI - Invoice Field Extraction
# Convolve 4.0 - Pan-IIT AI/ML Hackathon @ IIT Guwahati

# Core Dependencies
numpy>=1.24.0
Pillow>=10.0.0
loguru>=0.7.0
python-dotenv>=1.0.0  # Load environment variables from .env file

# PDF Processing
pdf2image>=1.16.3
PyMuPDF>=1.23.0

# OCR (EasyOCR primary, PaddleOCR optional fallback)
easyocr>=1.7.0
# paddlepaddle>=2.5.0  # Optional: uncomment for PaddleOCR fallback
# paddleocr>=2.7.0     # Optional: has compatibility issues with Python 3.13

# Object Detection
ultralytics>=8.1.0
opencv-python>=4.8.0

# Text Matching
rapidfuzz>=3.5.0

# VLM (Optional - for enhanced extraction)
# Supports both OpenAI and Azure OpenAI (same package)
openai>=1.0.0
# transformers>=4.36.0  # Uncomment for local Qwen

# Azure OpenAI Configuration (set as environment variables):
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_API_KEY=your-api-key
# AZURE_OPENAI_DEPLOYMENT=gpt-4o

# Data Processing
pandas>=2.0.0